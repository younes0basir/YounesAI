# Base URL of the YounesAI Express backend (no trailing slash, no /api suffix).
# Simulator/emulator: http://localhost:3000
# Physical device / APK: http://YOUR_PUBLIC_IP:3000 (also set in mobile/eas.json for EAS builds)
EXPO_PUBLIC_API_URL=http://84.8.220.241:3000
