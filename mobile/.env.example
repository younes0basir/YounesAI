# Base URL of the YounesAI Express backend (no trailing slash, no /api suffix).
# Simulator/emulator: http://localhost:3000
# Physical device / APK: http://YOUR_PUBLIC_IP (port 80 via nginx; also set in mobile/eas.json)
EXPO_PUBLIC_API_URL=http://84.8.220.241
