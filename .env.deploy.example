# Copy to .env.deploy (gitignored) and adjust paths.
# Used by scripts/deploy-oracle.ps1 and scripts/deploy.sh

ORACLE_HOST=opc@84.8.220.241
ORACLE_KEY=C:\Users\ken\Downloads\ssh-key-2026-08-28.key
ORACLE_APP_DIR=/home/opc/YounesAI
BRANCH=main

# Upload local backend/.env before remote git pull (keeps secrets off GitHub)
SYNC_ENV=true

# Health checks after deploy
PUBLIC_HEALTH_URL=https://84-8-220-241.sslip.io/api/health

# Subscription plans (MVP — manual admin assignment)
ADMIN_API_KEY=
PLAN_STARTER_AI_DAILY=10
PLAN_PRO_AI_DAILY=100
PLAN_PLATINUM_AI_DAILY=500

# Oracle Cloud Security List — open BOTH ingress rules:
#   TCP 80  (HTTP + ACME cert renewal)
#   TCP 443 (HTTPS — required for Android; plain HTTP to IP is blocked on many phones)
